const generate_button = document.getElementById("generate-button"),
download_bar = document.getElementById('download-bar');

generate_button.addEventListener('click',()=>{
download_bar.style.display = 'flex';
})

